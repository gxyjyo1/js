1、git add .
git add -A
git add -u

2、git fetch
git pull



3、git branch -d branchName   删除分支时，不能在该分支状态下删除

4、使用git diff查看各个区之间的差异
git diff 和 git diff --cached容易混淆
 
git diff 比较的是工作区和暂存区的差别
 
git diff --cached 比较的是暂存区和版本库的差别
 
git diff HEAD 可以查看工作区和版本库的差别